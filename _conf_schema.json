{
    "per_msg_count":{
        "description": "每轮获取的群消息数量",
        "type": "int",
        "hint": "每轮获取这么多的消息，从这些消息中抽取出目标群友的文本消息",
        "default": 200
    },
    "max_msg_count": {
        "description": "总共要获取的好友消息数量",
        "type": "int",
        "hint": "获取的消息数量到达此值时停止轮询。用于分析的消息量越多，画像越准确，但需要消耗更多时间和Token",
        "default": 500
    },
    "max_query_rounds": {
        "description": "获取群消息的最大轮数",
        "type": "int",
        "hint": "对于不爱说话的人时，轮询再多的消息，Ta的消息也凑不够，当达到最大轮数时，强制停止轮询",
        "default": 10
    },
    "system_prompt_template": {
        "description": "系统提示词模板",
        "type": "text",
        "hint": "用于指导LLM分析群友的性格，请使用{nickname}和{gender}来表示群友的昵称和性别",
        "default": "请根据 {nickname} 的聊天记录，分析{gender}的性格特点, 并给出性格标签, 注意要用可爱、调侃的语气，尽量夸奖这位群友，注意给出你的分析过程"
    }
}
